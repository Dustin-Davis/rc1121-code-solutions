/* exported getFirstChar */

// define a function named 'getFirsdtChar' which has one parameter 'string'
// create a string and assign it to a variable 'char'
// make a for loop and then have a var i = 0 then a condition run i <= string.length then have a final expression i++
// use an expression to add variable char to the string[0] then assign new value back to char
// return the char string

function getFirstChar(string) {
  var char = '';
  for (var i = 0; i <= 0; i++) {
    char += string[0];
  }
  return char;
}

// define a function named 'getFirsdtChar' which has one parameter 'string'
// create a string and assign it to a variable 'char'
// make a for loop and then have a var i = 0 then a condition run i <= 0 then have a final expression i++
// use an expression to add variable char to the string[0] then assign new value back to char
// return the char string
