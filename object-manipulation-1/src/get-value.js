/* exported getValue */

// define a function with a name 'getValue' with to parameter 'object' 'key'
// have a empty array assign to a variable name value
// have a for..in loop to pull the the first object of the key
// return the variable value

function getValue(object, key) {
  var value = object[key];
  return value;
}

// define a function with a name 'getValue' with to parameter 'object' 'key'
// have object at key and assign it to a varaible 'value'
// return value
